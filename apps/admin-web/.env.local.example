# URL del backend admin (apps/admin) — solo accesible server-side vía el proxy
# En desarrollo: http://localhost:8001
# En producción: URL del servicio Railway del admin API
ADMIN_API_URL=http://localhost:8001
